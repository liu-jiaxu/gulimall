<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.gulimall.product.dao.AttrDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.atguigu.gulimall.product.entity.AttrEntity" id="attrMap">
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="searchType" column="search_type"/>
        <result property="icon" column="icon"/>
        <result property="valueSelect" column="value_select"/>
        <result property="attrType" column="attr_type"/>
        <result property="enable" column="enable"/>
        <result property="catelogId" column="catelog_id"/>
        <result property="showDesc" column="show_desc"/>
    </resultMap>

    <!-- 分页查询指定属性分组【未关联】的属性（MyBatis-Plus 分页插件自动拼 LIMIT/COUNT） -->
    <select id="selectNoRelationAttr" resultMap="attrMap">
        # 太懒了直接a.*了
        SELECT a.*
        FROM pms_attr a
        WHERE a.attr_type = 1
          AND a.catelog_id IN (
                SELECT g.catelog_id
                FROM pms_attr_group g
                WHERE g.attr_group_id = #{attrGroupId}
          )
          AND NOT EXISTS (
                SELECT 1
                FROM pms_attr_attrgroup_relation r
                WHERE r.attr_id = a.attr_id
          )
        <if test="key != null and key != ''">
          AND ( a.attr_id = #{key} OR a.attr_name LIKE CONCAT('%', #{key}, '%') )
        </if>
    </select>

	<select id="getAttrNameById" resultType="java.lang.String">
        SELECT a.attr_name
        FROM pms_attr a
        WHERE a.attr_id = #{attrId}
	</select>

</mapper>